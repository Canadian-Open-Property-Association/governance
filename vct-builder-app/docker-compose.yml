services:
  vct-builder:
    build: .
    ports:
      - "5173:5173"
      - "5174:5174"
    volumes:
      - .:/app
      - /app/node_modules
      - vct-assets:/app/assets
    environment:
      - NODE_ENV=development

volumes:
  vct-assets:
